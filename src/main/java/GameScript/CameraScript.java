package GameScript;

import GameConfig.gameConfig;
import GameObject.GameObject;

/**
 * …„”∞ª˙¿‡
 */
public class CameraScript extends EngineBehavior implements gameConfig {

    public CameraScript(GameObject go) {
        super(go);
    }

    @Override
    public void Update() {
//		if (Input.GetKeyDown(KeyEvent.VK_UP)) {
//			transform.translate(new Vector2(0, 20).multi(Time.DeltaTime));
//		}
//
//		if (Input.GetKeyDown(KeyEvent.VK_LEFT)) {
//			transform.translate(new Vector2(-20, 0).multi(Time.DeltaTime));
//		}
//
//		if (Input.GetKeyDown(KeyEvent.VK_DOWN)) {
//			transform.translate(new Vector2(0, -20).multi(Time.DeltaTime));
//		}
//
//		if (Input.GetKeyDown(KeyEvent.VK_RIGHT)) {
//			transform.translate(new Vector2(20, 0).multi(Time.DeltaTime));
//		}
//		

//        if (Input.GetKeyDown(KeyEvent.VK_E)) {
//            Screen.PX += 20 * Time.DeltaTime;
//        }
//
//        if (Input.GetKeyDown(KeyEvent.VK_Q)) {
//            Screen.PX -= 20 * Time.DeltaTime;
//        }
    }
}
